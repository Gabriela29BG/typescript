
    // Declaración e inicialización de la variable 'isEnable' con el valor booleano true.
    let isEnable = true;
    // Asignación de un nuevo valor booleano (false) a la variable 'isEnable'.
    isEnable = false;
  
    // Declaración e inicialización de la variable 'isNew' con el tipo de dato booleano y el valor false.
    let isNew: boolean = false;
    // Imprimir en la consola el valor actual de 'isNew'.
    console.log('isNew', isNew);
    // Asignación de un nuevo valor booleano (true) a la variable 'isNew'.
    isNew = true;
    // Imprimir en la consola el valor actual de 'isNew'.
    console.log('isNew', isNew);
  
    // Generación de un número aleatorio entre 0 (inclusive) y 1 (exclusive) y asignación a la constante 'random'.
    const random = Math.random();
    // Imprimir en la consola el valor actual de 'random'.
    console.log('random', random);
    // Asignación de un nuevo valor booleano a 'isNew' basado en una condición ternaria.
    isNew = random >= 0.5 ? true : false;
    // Imprimir en la consola el valor actual de 'isNew'.
    console.log('isNew', isNew);
  
    // Declaración de una constante 'myBoolean' con el tipo de dato booleano y el valor true.
    const myBoolean: boolean = true;
  
  